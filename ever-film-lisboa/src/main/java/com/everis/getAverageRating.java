package com.everis;

/*public class getAverageRating {
		  int sum = 0;
		  for (int i=0;
		private Object people; i < people.length; i++)
		  {
		       int[] people;
			sum = sum + people[i];
		  }
		  double average = sum / (double)people.length;
		  return average;
		}
*/